---
layout: page
title: 📚 Stanford CS336
permalink: /cs336/
---

<style>
.collection-header {
  text-align: center;
  padding: 2em 0;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 12px;
  margin-bottom: 2em;
}

.collection-header h1 {
  margin: 0;
  font-size: 2.5em;
}

.collection-header p {
  font-size: 1.2em;
  margin: 0.5em 0 0 0;
  opacity: 0.9;
}

.posts-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1.5em;
  margin: 2em 0;
}

.post-card {
  border: 1px solid var(--border-color);
  border-radius: 8px;
  padding: 1.5em;
  background-color: var(--bg-color);
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px var(--shadow);
}

.post-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 4px 16px var(--shadow);
}

.post-card h3 {
  margin-top: 0;
  font-size: 1.3em;
}

.post-card .post-date {
  color: #666;
  font-size: 0.9em;
  margin-bottom: 0.5em;
}

.post-card .post-excerpt {
  color: var(--text-color);
  line-height: 1.6;
}

.post-card a {
  text-decoration: none;
  color: var(--link-color);
}

.post-card a:hover {
  text-decoration: underline;
}

[data-theme="dark"] .collection-header {
  background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
}

[data-theme="dark"] .post-card .post-date {
  color: #a0aec0;
}
</style>

{% assign published_cs336 = site.data.published_posts.cs336 | size %}

<div class="collection-header">
  <h1>📚 Stanford CS336</h1>
  <p>Language Modeling from Scratch | 语言模型从零开始</p>
  <p style="font-size: 0.9em; margin-top: 1em;">{{ published_cs336 }} posts</p>
</div>

## 关于这个系列 About This Series

深入学习 Stanford CS336 课程，从零开始构建语言模型。涵盖tokenization、transformer架构、训练技巧等核心主题。

In-depth exploration of Stanford CS336 course, building language models from scratch. Covers core topics including tokenization, transformer architecture, and training techniques.

---

## 📝 所有文章 All Posts

{% assign published_list = site.data.published_posts.cs336 %}
{% assign published_posts = "" | split: "" %}
{% for post in site.cs336 %}
  {% assign filename = post.path | split: "/" | last %}
  {% if published_list contains filename %}
    {% assign published_posts = published_posts | push: post %}
  {% endif %}
{% endfor %}

<div class="posts-grid">
{% assign sorted_posts = published_posts | sort: 'date' | reverse %}
{% if sorted_posts.size > 0 %}
  {% for post in sorted_posts %}
    <div class="post-card">
      <div class="post-date">{{ post.date | date: "%Y-%m-%d" }}</div>
      <h3><a href="{{ post.url | relative_url }}">{{ post.title }}</a></h3>
    </div>
  {% endfor %}
{% else %}
  <p style="text-align: center; color: #666; font-style: italic;">Coming soon... 即将推出...</p>
{% endif %}
</div>
